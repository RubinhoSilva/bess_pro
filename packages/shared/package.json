{
  "name": "@bess-pro/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build --verbose",
    "build:clean": "tsc --build --clean && tsc --build --verbose",
    "dev": "tsc --build --watch",
    "clean": "tsc --build --clean && rm -rf dist",
    "verify": "tsc --noEmit --skipLibCheck"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}
